service {
  console {
    enabled = true
  }
  akka {
    enabled = true
    host = "localhost"
    port = 8080
  }
}

db = {
  driver = "org.postgresql.Driver"
  url = "jdbc:postgresql://localhost:5432/tinkoff"
  user = "tinkoff"
  pass = "tinkoff"
}